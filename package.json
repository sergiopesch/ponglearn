{
  "name": "ponglearn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/app.js && node --check src/learning.js",
    "test": "node --test",
    "validate": "npm run check && npm test",
    "serve": "python3 -m http.server ${PORT:-4173}"
  }
}
